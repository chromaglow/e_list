---
phase: 02-core-listing-lifecycle
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - components/listings/ListingCard.tsx
autonomous: true
requirements:
  - LIST-03
  - LIST-04
  - UX-01

must_haves:
  truths:
    - "ListingCard renders a photo using next/image with fill, loading=lazy, and sizes; falls back to /fallback.jpg when photo_key is null (D-09)"
    - "ListingCard renders a FREE badge (emerald-100/emerald-800) when listing.price is null/empty, or the price string when set (UX-01)"
    - "ListingCard renders created_at as a human date via toLocaleDateString wrapped in a <time> element with dateTime ISO string (LIST-04)"
    - "ListingCard renders title, description with line-clamp-2, poster_name, and contact_info (LIST-03)"
    - "Card uses article element with rounded-lg border bg-card shadow-sm overflow-hidden (UI-SPEC §4.2)"
  artifacts:
    - path: "components/listings/ListingCard.tsx"
      provides: "Server Component card for a single active listing"
      exports: ["default (ListingCard)"]
      contains: "Listing"
  key_links:
    - from: "components/listings/ListingCard.tsx"
      to: "lib/schema.ts"
      via: "import { Listing } type"
      pattern: "from '@/lib/schema'"
    - from: "components/listings/ListingCard.tsx"
      to: "next/image"
      via: "import Image from 'next/image'"
      pattern: "from 'next/image'"
    - from: "components/listings/ListingCard.tsx"
      to: "public/fallback.jpg"
      via: "src={listing.photo_key ?? '/fallback.jpg'}"
      pattern: "fallback\\.jpg"
---

<objective>
Create the ListingCard Server Component that renders a single listing as a full-width card. The card must match UI-SPEC §4.2 exactly: 4:3 photo with lazy loading and fallback, title/price row, 2-line-clamped description, poster info, and date. This is a pure rendering component — no state, no event handlers, no 'use client'.

Purpose: ListingCard is the visual core of Phase 2. Every active listing in the browse page is rendered as a ListingCard. Getting it right here — including the FREE badge, date format, and image fallback — means Plan 02-05 (browse page assembly) is just a data fetch + map.

Output: components/listings/ListingCard.tsx — a Server Component ready to receive a Listing prop and render it.
</objective>

<execution_context>
@C:/Users/ezras/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ezras/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/ezras/OneDrive/Documents/work/GitHub/e_list/.planning/PROJECT.md
@C:/Users/ezras/OneDrive/Documents/work/GitHub/e_list/.planning/ROADMAP.md
@C:/Users/ezras/OneDrive/Documents/work/GitHub/e_list/.planning/phases/02-core-listing-lifecycle/02-CONTEXT.md
@C:/Users/ezras/OneDrive/Documents/work/GitHub/e_list/.planning/phases/02-core-listing-lifecycle/02-UI-SPEC.md
@C:/Users/ezras/OneDrive/Documents/work/GitHub/e_list/.planning/phases/02-core-listing-lifecycle/02-PATTERNS.md
@C:/Users/ezras/OneDrive/Documents/work/GitHub/e_list/.planning/phases/02-core-listing-lifecycle/02-01-SUMMARY.md
@C:/Users/ezras/OneDrive/Documents/work/GitHub/e_list/.planning/phases/02-core-listing-lifecycle/02-02-SUMMARY.md

<interfaces>
<!-- Key types the executor needs. Extracted from codebase. -->

From lib/schema.ts:
```typescript
export const listings = sqliteTable('listings', {
  id:           text('id').primaryKey(),
  title:        text('title').notNull(),
  description:  text('description').notNull(),
  price:        text('price'),                   // nullable — null means FREE
  poster_name:  text('poster_name').notNull(),
  contact_info: text('contact_info').notNull(),
  photo_key:    text('photo_key'),               // nullable — null means use /fallback.jpg
  edit_token:   text('edit_token').notNull(),
  status:       text('status', { enum: ['active', 'taken', 'deleted'] }).notNull().default('active'),
  created_at:   integer('created_at', { mode: 'timestamp' }).notNull(),  // Drizzle returns Date object
  updated_at:   integer('updated_at', { mode: 'timestamp' }).notNull(),
  taken_at:     integer('taken_at',   { mode: 'timestamp' }),
  deleted_at:   integer('deleted_at', { mode: 'timestamp' }),
})

export type Listing    = typeof listings.$inferSelect
export type NewListing = typeof listings.$inferInsert
```

From components/shell/EmptyState.tsx (Server Component structural reference):
```typescript
// No 'use client' directive — Server Component default
export default function EmptyState() {
  return (
    <div className="mx-auto max-w-screen-sm px-4 py-12 text-center">
      ...
    </div>
  )
}
```

From UI-SPEC §4.2 — full card class specification (source of truth):
Article: `rounded-lg border bg-card shadow-sm overflow-hidden`
Photo container: `relative aspect-[4/3] w-full bg-muted`
Image props: fill, className="object-cover", loading="lazy", sizes="(max-width: 640px) 100vw, 640px"
Content area: `px-4 pt-3 pb-4`
Title row: `flex items-start justify-between gap-3`
  - h2: `text-base font-semibold leading-snug`
  - price span: `text-sm font-normal shrink-0`
  - FREE badge: `inline-flex items-center rounded-full bg-emerald-100 px-2.5 py-0.5 text-xs font-semibold text-emerald-800 dark:bg-emerald-900/30 dark:text-emerald-400 shrink-0`
Description: `mt-1.5 text-sm text-muted-foreground line-clamp-2`
Poster row: `mt-3 text-sm` with separator dot `mx-1.5 text-muted-foreground`
Date: `mt-1 block text-xs text-muted-foreground` inside `<time dateTime={iso}>`
Date format: `toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create components/listings/ListingCard.tsx</name>
  <files>components/listings/ListingCard.tsx</files>
  <read_first>
    - C:/Users/ezras/OneDrive/Documents/work/GitHub/e_list/lib/schema.ts — Listing type, column names and nullability
    - C:/Users/ezras/OneDrive/Documents/work/GitHub/e_list/.planning/phases/02-core-listing-lifecycle/02-UI-SPEC.md — §4.2 full card anatomy (source of truth for class strings); §3.2 FREE badge exact class string; §6.7 date format
    - C:/Users/ezras/OneDrive/Documents/work/GitHub/e_list/components/shell/EmptyState.tsx — Server Component pattern (no 'use client')
    - C:/Users/ezras/OneDrive/Documents/work/GitHub/e_list/.planning/phases/02-core-listing-lifecycle/02-PATTERNS.md — ListingCard.tsx adaptation notes section
  </read_first>
  <action>
    Create components/listings/ListingCard.tsx as a Server Component (no 'use client' directive):

    Imports: `Image` from `next/image`; `Listing` type from `@/lib/schema`.

    Add a local helper function `formatDate(date: Date): string` that calls `date.toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })`.

    Props: `({ listing }: { listing: Listing })`.

    Outer wrapper: `<article className="rounded-lg border bg-card shadow-sm overflow-hidden">`.

    Photo section: `<div className="relative aspect-[4/3] w-full bg-muted">` wrapping `<Image src={listing.photo_key ?? '/fallback.jpg'} alt={listing.title} fill className="object-cover" loading="lazy" sizes="(max-width: 640px) 100vw, 640px" />`. Note: photo_key stores the full Vercel Blob CDN URL (per OQ-01 resolution in RESEARCH.md) — pass it directly to Image src.

    Content div: `<div className="px-4 pt-3 pb-4">`.

    Title row: `<div className="flex items-start justify-between gap-3">` containing `<h2 className="text-base font-semibold leading-snug">{listing.title}</h2>` and conditional price/FREE: when `listing.price` is truthy, render `<span className="text-sm font-normal shrink-0">{listing.price}</span>`; when falsy (null or empty string), render `<span className="inline-flex items-center rounded-full bg-emerald-100 px-2.5 py-0.5 text-xs font-semibold text-emerald-800 dark:bg-emerald-900/30 dark:text-emerald-400 shrink-0">FREE</span>`.

    Description: `<p className="mt-1.5 text-sm text-muted-foreground line-clamp-2">{listing.description}</p>`.

    Poster info: `<div className="mt-3 text-sm">` with `<span className="font-normal">{listing.poster_name}</span>`, `<span className="mx-1.5 text-muted-foreground">·</span>`, `<span className="text-muted-foreground">{listing.contact_info}</span>`.

    Date: `<time dateTime={listing.created_at.toISOString()} className="mt-1 block text-xs text-muted-foreground">{formatDate(listing.created_at)}</time>`.

    Do NOT add a status badge — the browse page only shows active listings and Phase 3 handles status display (per UI-SPEC §3.3).

    Do NOT add 'use client' — this is a pure Server Component.

    Do NOT use font-medium anywhere — only font-normal and font-semibold (per UI-SPEC §2.2).
  </action>
  <verify>
    <automated>cd C:/Users/ezras/OneDrive/Documents/work/GitHub/e_list && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - components/listings/ListingCard.tsx does NOT contain the string 'use client'
    - components/listings/ListingCard.tsx contains `import Image from 'next/image'`
    - components/listings/ListingCard.tsx contains `import { Listing }` (or `import type { Listing }`) from `'@/lib/schema'`
    - components/listings/ListingCard.tsx contains `loading="lazy"`
    - components/listings/ListingCard.tsx contains `sizes="(max-width: 640px) 100vw, 640px"`
    - components/listings/ListingCard.tsx contains `listing.photo_key ?? '/fallback.jpg'`
    - components/listings/ListingCard.tsx contains `bg-emerald-100` and `text-emerald-800` for the FREE badge
    - components/listings/ListingCard.tsx contains `line-clamp-2` on the description paragraph
    - components/listings/ListingCard.tsx contains `toLocaleDateString('en-US'`
    - components/listings/ListingCard.tsx contains `<time` with `dateTime={listing.created_at.toISOString()}`
    - components/listings/ListingCard.tsx contains `<article` as the card wrapper
    - components/listings/ListingCard.tsx does NOT contain `font-medium`
    - `npx tsc --noEmit` exits 0 after file creation
  </acceptance_criteria>
  <done>ListingCard.tsx is a TypeScript-clean Server Component matching UI-SPEC §4.2 exactly: photo with lazy loading and fallback, FREE badge or price, 2-line-clamped description, poster name/contact, formatted date in time element.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB data → ListingCard render | Untrusted user content (listing title, description, poster_name, contact_info) rendered in JSX |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-A | Tampering (XSS) | ListingCard — rendering user-supplied title, description, contact_info | mitigate | React automatically HTML-escapes all content in JSX text positions — no `dangerouslySetInnerHTML` is used; no raw HTML injection possible |
| T-02-03-B | Information Disclosure | edit_token rendered in card | mitigate | ListingCard does not render listing.edit_token — that column is never referenced in the component; confirmed by absence of `edit_token` in JSX |
| T-02-03-C | Tampering | photo_key contains attacker-controlled URL passed to next/image | accept | photo_key is a Vercel Blob CDN URL stored server-side; next/image remotePatterns restricts which hostnames are allowed (*.blob.vercel-storage.com); any non-matching URL results in a Next.js 400 error, not a security breach |
| T-02-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- components/listings/ListingCard.tsx exists and exports a default function
- File does not contain 'use client'
- File contains loading="lazy", sizes prop, fallback.jpg reference, bg-emerald-100, line-clamp-2, toLocaleDateString, time element with dateTime
- File does not render listing.edit_token
- File does not contain font-medium
</verification>

<success_criteria>
- ListingCard.tsx is a Server Component matching UI-SPEC §4.2 exactly
- Photo renders from photo_key or falls back to /fallback.jpg (D-09)
- FREE badge appears when price is null/empty (UX-01)
- Date is formatted as "May 18, 2026" and wrapped in time element with ISO dateTime (LIST-04)
- Title, description (2-line clamped), poster name, contact info all rendered (LIST-03)
- TypeScript clean
</success_criteria>

<output>
Create `.planning/phases/02-core-listing-lifecycle/02-03-SUMMARY.md` when done.
</output>
