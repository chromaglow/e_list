---
phase: 02-core-listing-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - public/fallback.jpg
autonomous: true
requirements:
  - UX-03

must_haves:
  truths:
    - "next.config.ts contains images.remotePatterns for *.blob.vercel-storage.com so next/image accepts Vercel Blob CDN URLs"
    - "A fallback image exists at /public/fallback.jpg so listing cards always show an image when photo_key is null"
  artifacts:
    - path: "next.config.ts"
      provides: "images.remotePatterns whitelist for Vercel Blob CDN"
      contains: "blob.vercel-storage.com"
    - path: "public/fallback.jpg"
      provides: "Static fallback image for listings with no photo"
  key_links:
    - from: "next.config.ts"
      to: "components/listings/ListingCard.tsx"
      via: "next/image remotePatterns"
      pattern: "blob\\.vercel-storage\\.com"
---

<objective>
Add the two prerequisites that every listing card depends on: (1) whitelist the Vercel Blob CDN hostname in next.config.ts so next/image can serve uploaded photos, and (2) add a /public/fallback.jpg placeholder so cards always render an image even when photo_key is null (D-09).

Purpose: Without remotePatterns, next/image throws a 400 for any blob URL. Without fallback.jpg, cards with no photo_key crash or show broken images. Both must exist before ListingCard is built.

Output: Modified next.config.ts with images.remotePatterns; new public/fallback.jpg placeholder.
</objective>

<execution_context>
@C:/Users/ezras/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ezras/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/ezras/OneDrive/Documents/work/GitHub/e_list/.planning/PROJECT.md
@C:/Users/ezras/OneDrive/Documents/work/GitHub/e_list/.planning/ROADMAP.md
@C:/Users/ezras/OneDrive/Documents/work/GitHub/e_list/.planning/STATE.md
@C:/Users/ezras/OneDrive/Documents/work/GitHub/e_list/.planning/phases/02-core-listing-lifecycle/02-CONTEXT.md
@C:/Users/ezras/OneDrive/Documents/work/GitHub/e_list/.planning/phases/02-core-listing-lifecycle/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From next.config.ts (current full content):
```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add images.remotePatterns to next.config.ts</name>
  <files>next.config.ts</files>
  <read_first>
    - C:/Users/ezras/OneDrive/Documents/work/GitHub/e_list/next.config.ts — current config (modify, not replace; preserve any existing options)
  </read_first>
  <action>
    Replace the empty `/* config options here */` comment in nextConfig with an `images` block containing a `remotePatterns` array. The single entry: protocol "https", hostname "*.blob.vercel-storage.com", port "" (empty string), pathname "/**". Do not add any other config keys. Keep the existing `import type { NextConfig } from "next"` import and `export default nextConfig` unchanged.
  </action>
  <verify>
    <automated>node -e "const c = require('./next.config.ts'); console.log(JSON.stringify(c))" 2>/dev/null || npx tsx -e "import c from './next.config.ts'; console.log(JSON.stringify(c.default ?? c))"</automated>
  </verify>
  <acceptance_criteria>
    - next.config.ts contains the string `*.blob.vercel-storage.com`
    - next.config.ts contains `remotePatterns`
    - next.config.ts contains `pathname: '/**'`
    - next.config.ts contains `protocol: 'https'`
    - The file still exports `nextConfig` as default
    - TypeScript compilation does not error: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>next.config.ts includes images.remotePatterns that whitelists *.blob.vercel-storage.com; tsc passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Add /public/fallback.jpg placeholder</name>
  <what-built>
    A placeholder image file must exist at public/fallback.jpg. This is a static asset that will be served by Next.js and displayed on listing cards when photo_key is null (D-09). The user intends to replace this with an actual photo of their dog before Phase 2 ships — for now a real placeholder JPEG (any valid JPEG, e.g. a 1x1 pixel, a solid-color square, or a downloaded placeholder image) is needed so the build and tests don't break and ListingCard renders without a broken image.

    Claude cannot create a valid binary JPEG from scratch. Instead:
    1. Download a tiny placeholder JPEG: `curl -L "https://placehold.co/640x480.jpg" -o public/fallback.jpg`
    2. If that fails (no network or curl unavailable), create a minimal valid JPEG using Node.js: `node -e "const fs=require('fs'); const jpg=Buffer.from([]); fs.writeFileSync('public/fallback.jpg', jpg)"`
  </what-built>
  <how-to-verify>
    1. Confirm public/fallback.jpg exists: `ls public/fallback.jpg` (or `dir public\fallback.jpg` on Windows)
    2. Confirm the file size is greater than 0 bytes
    3. Confirm it starts with JPEG magic bytes (FF D8): open with any image viewer, or run `node -e "const b=require('fs').readFileSync('public/fallback.jpg'); console.log(b[0].toString(16), b[1].toString(16))"`
       — expected output: `ff d8`
    4. Optionally: open the file in a browser to confirm it renders as an image (it may be a gray/black 1px image — that is fine)
  </how-to-verify>
  <resume-signal>Type "approved" once public/fallback.jpg exists and has JPEG magic bytes (ff d8). Or type "replace" if you want to supply your own fallback photo — in that case, save your image to public/fallback.jpg and type "approved".</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| next.config.ts → Next.js image optimizer | remotePatterns controls which external hostnames the image optimizer will proxy/serve |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-A | Tampering | next.config.ts remotePatterns wildcard | accept | Wildcard `*.blob.vercel-storage.com` is intentionally broad but scoped to Vercel's own CDN domain — no user-controlled subdomain injection possible; Vercel Blob generates subdomains, not users |
| T-02-01-B | Tampering | public/fallback.jpg static asset | accept | Static asset served by Next.js, not user-uploaded — no injection surface; file is committed to repo |
| T-02-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — N/A |
</threat_model>

<verification>
- `next.config.ts` contains `*.blob.vercel-storage.com` in remotePatterns
- `public/fallback.jpg` exists with JPEG magic bytes (FF D8 header)
- `npx tsc --noEmit` exits 0 (TypeScript accepts the new config shape)
- `npm run build` does not error on next.config.ts parsing
</verification>

<success_criteria>
- next.config.ts has images.remotePatterns with the blob.vercel-storage.com wildcard entry
- public/fallback.jpg exists as a valid JPEG binary file
- No TypeScript errors introduced
- Plan 02-03 (ListingCard) can proceed without image-related Next.js errors
</success_criteria>

<output>
Create `.planning/phases/02-core-listing-lifecycle/02-01-SUMMARY.md` when done.
</output>
